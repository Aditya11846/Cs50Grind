#include <stdio.h>
#include <cs50.h>

int main(void)
{
    long number = get_long("Enter the number\n");

    long temp_number = number;
    int length_count;

    while(temp_number>0)
    {
        temp_number/10
    }
}
